enum Direction {
  Up = 1,
  Down,
  Left,
  Right
}
enum FileAccess {
  None,
  Read =1 << 1,
}

const enum Directions {
  Up,
  Down,
  Left,
  Right
}

let directions = [Directions.Up, Directions.Down, Directions.Left, Directions.Right]